# Afshar text-SVC legacy runtime

This environment is intentionally isolated because the serialized artifacts were created with
scikit-learn 0.19.0. Build or run it only after internal-use permission is documented and static
pickle inspection has passed.

The model predicts a full ARDS phenotype from report text. It is not a bilateral-opacity model and
must remain outside the primary comparator ranking.
